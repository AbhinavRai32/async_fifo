# async_fifo
Parameterized asynchronous FIFO (depth=16, width=8) with gray-coded pointer CDC synchronization. Includes 2-flop reset and pointer synchronizers, and a self-checking testbench with dual independent clocks, a legality-aware reference model, and concurrent randomized read/write stimulus.
